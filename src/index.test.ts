import { ChineseNumber } from './index';

describe('Numbers', () => {
  it('TO DO', () => {
    expect(true).toBe(true);
    expect(ChineseNumber).toBeDefined();
  });
});

describe('Full sentences', () => {
  it('TO DO', () => {
    expect(true).toBe(true);
  });
});
